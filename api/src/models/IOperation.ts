export default interface IOperation{
    action: string;
    activityId: string;
    id: string;
    offerId: string;
    planId: string;
    publisherId: string;
    quantity: string;
    status: string;
    subscriptionId: string;
    timeStamp: string;
}
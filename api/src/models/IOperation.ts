export default interface IOperation{
    id: string;
    activityId: string;
    subscriptionId: string;
    offerId: string;
    publisherId: string;
    planId: string;
    quantity: string;
    action: string;
    timeStamp: string;
    status: string;
}